export const metadata = {
  title: "Luxe | Shop Backend",
  description: "Created by Omer Tech",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
